import { EntryGenerator } from "./$types";

export const entries: EntryGenerator = () => {
	return [
		{ mode: 'preparty' },
		{ mode: 'crazy' },
        { mode: 'hot' },
		{ mode: 'teams' },
		{ mode: 'christmas' },
	];
};